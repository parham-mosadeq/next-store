const Buttons = ({ id }) => {
  return (
    <div className='flex w-full'>
      <button className='bg-blue-700 w-full text-white px-2 py-1 rounded-sm shadow-md'>
        Add to cart
      </button>
    </div>
  );
};

export default Buttons;
